class ModelInfo:

    def __init__(self, usecase: str, model: str, version: str, timestamp: str):
        self.usecase = usecase
        self.model = model
        self.version = version
        self.timestamp = timestamp
